export enum ARROW_DIRECTION {
	TOP = 'top',
	RIGHT = 'right',
	BOTTOM = 'bottom',
	LEFT = 'left'
}
export type BorderSizeType = "full" | "center" | "right"
export interface CellSizeType {
	padding: string
	fontSize: string
	labelFontSize: string
}